
server:
  port: 8084

spring:
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/guli_oms?serverTimezone=CTT&useUnicode=true&characterEncoding=UTF-8&useSSL=false
    username: root
    password: mly19891020@

  redis:
    host: 192.168.43.73

mybatis-plus:
  global-config:
    db-config:
      id-type: auto

  mapper-locations: classpath:mapper/oms/**/*.xml

